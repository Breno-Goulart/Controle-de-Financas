# Ficheiros de log e de depuração
logs
*.log
npm-debug.log*

# Diretórios de dependências e de compilação
# A pasta node_modules contém todos os pacotes instalados
node_modules/
# A pasta dist é gerada a cada compilação e não deve ser versionada
dist/

# Ficheiros de variáveis de ambiente
# Mantenha as suas chaves e segredos fora do repositório
.env
.env.*
!.env.example

# Ficheiros de cache e temporários do Vite e do sistema
.cache
.parcel-cache
*.tsbuildinfo
vite.config.js.timestamp-*

# Ficheiros de sistema operativo
.DS_Store
Thumbs.db