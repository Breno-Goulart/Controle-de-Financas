# netlify.toml
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20" # Esta linha é importante

[[redirects]]
  from = "/"
  to = "/login.html"
  status = 302